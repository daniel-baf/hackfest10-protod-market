SERVER URI
http://localhost:5000/

PROJECT
python 3.10
pycharm enterprise
postman app

HTML
JINJA
templates folder

DEPENDENCIES
PATH: (venv) [current_path]$
~$ python -m pip install flask
~$ pip install -U flask-cors
~$ pip install --upgrade pip
~$ export FLASK_APP=server.py
~$ export FLASK_ENV=development
~$ export FLASK_DEBUG=1
~$ flask run

INFO
use 'flask run' on console, so server will auto reload each time a file is changed